const app=document.getElementById('resultApp');
const qs=new URLSearchParams(location.search);const token=qs.get('t')||'';
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
function money(n){return new Intl.NumberFormat('he-IL',{style:'currency',currency:'ILS',maximumFractionDigits:0}).format(Number(n||0))}
function categoryLabel(k){return({general:'רכב ודירה',health:'בריאות וסיעוד',life:'חיים',other:'אחר'})[k]||k}
function fail(text){app.innerHTML=`<section class="result-error-card"><span class="result-error-icon">!</span><h1>לא ניתן לפתוח את התוצאה</h1><p>${esc(text)}</p><a class="primary" href="./">חזרה לאתר</a></section>`}
async function load(){if(!token||token.length<20)return fail('הקישור אינו תקין.');try{const res=await window.PensionBackend.callFunction({action:'get_result',token});render(res)}catch(e){console.error(e);fail(e.message==='RESULT_EXPIRED'?'תוקף הקישור הסתיים. ניתן לפנות אלינו לקבלת קישור חדש.':'הקישור אינו זמין או שפג תוקפו.')}}
function render(res){
 const a=res.insurance;if(!a)return fail('עדיין אין תוצאה זמינה לבדיקה זו.');
 const total=Math.max(a.annualTotal,1);const general=Math.round((a.categories?.general||0)/total*100),health=Math.round((a.categories?.health||0)/total*100),life=Math.round((a.categories?.life||0)/total*100),other=Math.max(0,100-general-health-life);const reviewCount=(a.insights||[]).filter(x=>x.level==='warn').length;const categoryRows=Object.entries(a.categories||{}).filter(([,v])=>v>0).sort((x,y)=>y[1]-x[1]);const topPolicies=(a.policyList||[]).slice(0,7);
 app.innerHTML=`<div class="public-result-wrap"><section class="public-result-hero"><span class="dashboard-mode real-pill">הבדיקה שלך מוכנה</span><h1>${res.firstName?`היי ${esc(res.firstName)}, `:''}הנה התמונה הביטוחית שלך</h1><p>ריכזנו את הדוח למספרים ברורים וסימנו נושאים שכדאי לבדוק לעומק.</p></section><div class="insurance-kpi-grid"><article class="insurance-kpi"><small>עלות שנתית משוערת</small><strong>${money(a.annualTotal)}</strong></article><article class="insurance-kpi"><small>ממוצע חודשי</small><strong>${money(a.monthlyAverage)}</strong></article><article class="insurance-kpi"><small>פוליסות מרכזיות</small><strong>${a.policyCount||0}</strong></article><article class="insurance-kpi"><small>נקודות שדורשות בדיקה</small><strong>${reviewCount}</strong></article></div><section class="insurance-summary-grid"><article class="insurance-donut-card"><span class="card-kicker">חלוקת הפרמיות</span><h2>לאן הולך הכסף?</h2><div class="insurance-donut" style="--general:${general};--health:${health};--life:${life};--other:${other}"><div class="insurance-donut-center"><small>סה״כ לשנה</small><strong>${money(a.annualTotal)}</strong></div></div><div class="insurance-legend">${categoryRows.map(([k,v])=>`<div class="insurance-legend-row"><i style="background:${k==='general'?'#1539d4':k==='health'?'#6b63e8':k==='life'?'#f59e0b':'#cbd5e1'}"></i><span>${categoryLabel(k)}</span><strong>${money(v)}</strong></div>`).join('')}</div></article><article class="policy-card"><span class="card-kicker">סיכום מהיר</span><h2>מה בולט בתיק?</h2><div class="policy-list">${(a.insights||[]).slice(0,4).map(x=>`<div class="policy-row"><div><strong>${esc(x.title)}</strong><small>${esc(x.text)}</small></div><span>${x.level==='warn'?'⚠':'✓'}</span></div>`).join('')}</div></article></section><section class="recommendation-section"><div class="section-title-row"><div><span class="card-kicker">בדיקה חכמה</span><h2>נקודות שכדאי לעבור עליהן</h2></div><span class="recommendation-count">${reviewCount} דורשות תשומת לב</span></div><div class="recommendation-list-modern">${(a.insights||[]).map((x,i)=>`<article class="recommendation-item ${x.level==='info'?'good':esc(x.level)}"><span class="recommendation-num">${String(i+1).padStart(2,'0')}</span><div><h3>${esc(x.title)}</h3><p>${esc(x.text)}</p>${x.level==='warn'?'<div class="insight-note">זו נקודה לבדיקה בלבד — לא המלצה לבצע שינוי אוטומטי.</div>':''}</div></article>`).join('')}</div></section><section class="all-insights-section"><div class="section-title-row"><div><span class="card-kicker">ריכוז לפי מספר פוליסה</span><h2>הפוליסות המרכזיות</h2></div><span class="recommendation-count">${a.policyCount||0} פוליסות</span></div><div class="policy-card"><div class="policy-list">${topPolicies.map(p=>`<div class="policy-row"><div><strong>${esc(p.company)} · ${esc(p.main)}</strong><small>${esc(p.policyMasked)} · ${(p.coverages||[]).length} כיסויים/רכיבים${p.period?' · '+esc(p.period):''}</small></div><strong>${money(p.annual)} / שנה</strong></div>`).join('')}</div></div></section><section class="result-cta"><div><span class="card-kicker">אפשר לעבור על זה יחד</span><h2>רוצה לבדוק איך אפשר לשפר את התיק הפנסיוני והביטוחי שלך?</h2><p>נעבור איתך על התוצאות, נבדוק אילו נקודות באמת רלוונטיות עבורך ונעזור להבין מה כדאי לבחון לעומק.</p></div><div class="result-cta-actions"><button class="primary result-callback-btn" id="callbackBtn">רוצה שיחזרו אליי</button><a class="secondary result-phone-btn" href="tel:048220228">04-8220228</a></div><div id="callbackMsg" class="backend-status hidden"></div></section><div class="insurance-source-note"><strong>איך חושב הסיכום?</strong> פרמיה חודשית הומרה לעלות שנתית ×12; פרמיה שנתית נשארה כפי שהיא. רכיבים בעלי אותו מספר פוליסה מאוחדים כדי לא לספור כל כיסוי כפוליסה נפרדת.</div><div class="disclaimer modern-disclaimer"><strong>גילוי נאות:</strong> התובנות הן כלי סינון והסבר בלבד. אין בהן המלצה לבטל, לרכוש או לשנות פוליסה. בפוליסות ותיקות, סיעוד ובריאות יש לבצע בדיקה מקצועית לפני שינוי.</div></div>`;
 document.getElementById('callbackBtn').addEventListener('click',requestCallback)
}
async function requestCallback(){const btn=document.getElementById('callbackBtn');const msg=document.getElementById('callbackMsg');btn.disabled=true;btn.textContent='שולח…';try{await window.PensionBackend.callFunction({action:'request_callback',token});msg.textContent='הבקשה התקבלה ✓ נחזור אליך בהקדם.';msg.classList.remove('hidden');msg.classList.add('ok');btn.textContent='הבקשה התקבלה'}catch(e){msg.textContent='לא הצלחנו לשלוח את הבקשה. אפשר להתקשר אלינו ב־04-8220228.';msg.classList.remove('hidden');btn.disabled=false;btn.textContent='נסה שוב'}}
load();
